# @wpk/config

Shared lint, format and TypeScript baselines for every workspace package.
Extend `tsconfig.base.json`; import the ESLint and Prettier configs from the
root re-exports. Nothing here is site-specific.
