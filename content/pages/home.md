---
slug: home
title: Welcome
locale: en
cluster: pages/home
updatedAt: 2026-01-01
source:
  system: sample
---

<p>This is the front page of a site built with the WordPress-to-Astro kit. It is a WordPress <em>page</em>, migrated: the body is HTML exactly as WordPress rendered it, and it is published at <code>/</code> because <code>migration.config.ts</code> names <code>home</code> as the front page.</p>
<p>Everything on this site is sample content. A production build refuses to ship it, so replace it with the site you are migrating — see <code>PLAYBOOK.md</code>.</p>
