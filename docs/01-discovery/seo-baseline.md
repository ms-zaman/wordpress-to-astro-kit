# SEO baseline

**Status:** Draft
**Owner:** <name>
**Last updated:** <YYYY-MM-DD>
**Phase:** 01-discovery

What the live site tells search engines today — the thing the successor must
reproduce or consciously change.

## Method

_Plugin and export, or crawl. Date._

## Per-URL metadata

_Row count. How many titles are hand-written versus template-generated; how
many descriptions are hand-written. Seed `content/seo/overrides.json` from
this, one row per URL, each `preserve` until somebody decides otherwise._

## Sitewide

| | |
| --- | --- |
| `robots.txt` | |
| Sitemaps | |
| Canonical policy | |
| `og:image` default | |
| Structured data | |

## Known defects

_Defects are data. Each gets a row and a disposition — reproduce it, or fix it
deliberately. Silent improvement is as much a deviation as silent loss._
